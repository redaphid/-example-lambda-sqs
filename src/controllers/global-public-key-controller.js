// Generated by CoffeeScript 2.4.1
var GlobalPublicKeyController, debug;

debug = require('debug')('meshblu-core-protocol-adapter-http:global-public-key-controller');

GlobalPublicKeyController = class GlobalPublicKeyController {
  constructor({jobManager, jobToHttp}) {
    this.get = this.get.bind(this);
    this.jobManager = jobManager;
    this.jobToHttp = jobToHttp;
  }

  get(req, res) {
    var job;
    job = this.jobToHttp.httpToJob({
      request: req,
      jobType: 'GetGlobalPublicKey'
    });
    debug('dispatching request', job);
    return this.jobManager.do(job, (error, jobResponse) => {
      if (error != null) {
        return res.sendError(error);
      }
      return this.jobToHttp.sendJobResponse({jobResponse, res});
    });
  }

};

module.exports = GlobalPublicKeyController;
